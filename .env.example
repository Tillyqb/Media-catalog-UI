# Optional override for API base URL.
# Default in src/api.ts is /api, which works with Vite proxy in local dev.
VITE_API_BASE_URL=/api

# Dev proxy target for the backend service.
VITE_DEV_PROXY_TARGET=http://192.168.50.112:8001
